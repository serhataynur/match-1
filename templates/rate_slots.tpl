<div>
	<p class="slot_name">{$slot_name}</p>
	<input type="range" name="rate{$slot_nr}" id="rate{$slot_nr}" value="{$slot_value}" min="1" max="5">
	<output name="value{$slot_nr}" for="rate{$slot_nr}"></output>
</div>
<div class="description">{$slot_description}</div>
